---
phase: 01-foundation-and-application-shell
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/data/frameworks.ts
  - src/components/layout/AppLayout.tsx
  - src/components/layout/TopNav.tsx
  - src/components/layout/Breadcrumbs.tsx
  - src/components/dashboard/FrameworkCard.tsx
  - src/pages/DashboardPage.tsx
  - src/pages/ModulePage.tsx
  - src/App.tsx
autonomous: false
requirements:
  - FOUND-02
  - FOUND-03

must_haves:
  truths:
    - "Dashboard displays exactly 5 framework cards: GRI, ISSB, CSRD, ESRS, SBTi"
    - "Clicking any framework card navigates to that module's page at /module/{slug}"
    - "Top navigation bar is persistent across dashboard and module pages"
    - "Breadcrumbs show 'Dashboard > Framework Name' when inside a module page"
    - "Module page shows framework intro text and a two-panel layout skeleton"
    - "Cards have subtle lift/shadow on hover for polished interaction feedback"
    - "Grid is responsive: 3 columns on large screens, 2 on medium, 1 on mobile"
  artifacts:
    - path: "src/data/frameworks.ts"
      provides: "Static framework metadata (name, slug, description, icon) for all 5 frameworks"
      exports: ["frameworks", "Framework"]
    - path: "src/App.tsx"
      provides: "Route definitions: index -> DashboardPage, /module/:frameworkSlug -> ModulePage"
      contains: "Routes"
    - path: "src/components/layout/AppLayout.tsx"
      provides: "Shared layout with TopNav and Outlet"
      contains: "Outlet"
    - path: "src/components/layout/TopNav.tsx"
      provides: "Persistent top navigation bar with app branding and breadcrumbs"
      contains: "Sustainability Consulting Club"
    - path: "src/components/layout/Breadcrumbs.tsx"
      provides: "Dynamic breadcrumb from route params"
      contains: "useParams"
    - path: "src/components/dashboard/FrameworkCard.tsx"
      provides: "Clickable card component with hover lift effect"
      contains: "group-hover"
    - path: "src/pages/DashboardPage.tsx"
      provides: "Dashboard with responsive grid of 5 framework cards"
      contains: "grid-cols"
    - path: "src/pages/ModulePage.tsx"
      provides: "Module page with framework intro and two-panel skeleton"
      contains: "grid-cols-1 gap-8 lg:grid-cols-2"
  key_links:
    - from: "src/pages/DashboardPage.tsx"
      to: "src/data/frameworks.ts"
      via: "imports frameworks array and maps to FrameworkCard"
      pattern: "frameworks\\.map"
    - from: "src/components/dashboard/FrameworkCard.tsx"
      to: "/module/:frameworkSlug"
      via: "React Router Link wrapping the card"
      pattern: "Link to=.*module/"
    - from: "src/App.tsx"
      to: "src/pages/ModulePage.tsx"
      via: "Route path=\"module/:frameworkSlug\""
      pattern: "Route path=.*frameworkSlug"
    - from: "src/components/layout/Breadcrumbs.tsx"
      to: "src/data/frameworks.ts"
      via: "Looks up framework name by slug from route params"
      pattern: "frameworks\\.find"
    - from: "src/components/layout/AppLayout.tsx"
      to: "React Router Outlet"
      via: "Renders child routes via Outlet"
      pattern: "<Outlet"
---

<objective>
Build the complete dashboard UI with 5 clickable framework cards, persistent navigation with breadcrumbs, module pages with framework intros and two-panel skeletons, and client-side routing -- all styled with the forest/earth theme established in Plan 01.

Purpose: Deliver the user-facing application shell so students see a polished, professional dashboard and can navigate to any module page.
Output: A fully interactive dashboard at the root URL with routing to 5 module pages, all visually themed.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-application-shell/01-RESEARCH.md
@.planning/phases/01-foundation-and-application-shell/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create framework data, layout components, and route definitions</name>
  <files>
    src/data/frameworks.ts
    src/components/layout/AppLayout.tsx
    src/components/layout/TopNav.tsx
    src/components/layout/Breadcrumbs.tsx
    src/App.tsx
  </files>
  <action>
    Build the routing backbone and shared layout components that every page depends on.

    1. Create `src/data/frameworks.ts` -- the single source of truth for all 5 frameworks:
       - Export a `Framework` interface: `{ slug: string; name: string; shortName: string; description: string; icon: LucideIcon }`
       - Export a `frameworks` array with all 5 entries (use the exact data from RESEARCH.md Pattern 4):
         - GRI: slug "gri", icon Leaf, "Global Reporting Initiative standards for broad stakeholder-focused sustainability disclosure."
         - ISSB: slug "issb", icon BarChart3, "International Sustainability Standards Board framework for investor-focused financial materiality."
         - CSRD: slug "csrd", icon FileText, "EU Corporate Sustainability Reporting Directive requiring double materiality assessment."
         - ESRS: slug "esrs", icon ClipboardList, "European Sustainability Reporting Standards implementing CSRD disclosure requirements."
         - SBTi: slug "sbti", icon Target, "Science Based Targets initiative for validated emissions reduction target-setting."
       - Import ONLY the specific Lucide icons used (Leaf, BarChart3, FileText, ClipboardList, Target) -- not a barrel import.

    2. Create `src/components/layout/TopNav.tsx`:
       - Persistent top navigation bar rendered on every page.
       - Left side: App branding text -- "Sustainability Consulting Club (SCC)" as the primary text, with a subtle secondary line or delimiter before "PwC Case Study: Sustainability Reporting". Keep it minimal and clean, NOT a hero section. Use Link to "/" on the branding for Home navigation.
       - Right side: Breadcrumbs component (rendered conditionally -- hidden on dashboard, shown on module pages).
       - Style: `bg-card` background with a subtle bottom border (`border-b border-border`). Professional, not flashy. Use `max-w-7xl mx-auto` for content width consistency with main content area.
       - The nav should feel like a consulting firm's internal tool header.

    3. Create `src/components/layout/Breadcrumbs.tsx`:
       - Use shadcn/ui Breadcrumb component (Breadcrumb, BreadcrumbList, BreadcrumbItem, BreadcrumbLink, BreadcrumbPage, BreadcrumbSeparator).
       - Use `useLocation()` and `useParams()` from react-router.
       - If on root path ("/"), return null (no breadcrumbs on dashboard).
       - If on a module page, show: "Dashboard" (linked to /) > "{Framework Name}" (current page, not linked).
       - Look up framework name from the `frameworks` array using `frameworkSlug` param.
       - Follow RESEARCH.md Pattern 6 exactly.

    4. Create `src/components/layout/AppLayout.tsx`:
       - Renders `<TopNav />` followed by a `<main>` content area with `<Outlet />`.
       - Main area: `max-w-7xl mx-auto px-4 py-8 sm:px-6 lg:px-8` for consistent content width.
       - Wrapper div: `min-h-screen bg-background font-sans text-foreground`.
       - Follow RESEARCH.md Pattern 3.

    5. Update `src/App.tsx` with route definitions:
       - Import Routes, Route from "react-router".
       - Wrap all routes in `<AppLayout>` layout route (using `<Route element={<AppLayout />}>`).
       - Index route: `<DashboardPage />`
       - Module route: `path="module/:frameworkSlug"` -> `<ModulePage />`
       - Follow RESEARCH.md Pattern 1 for App.tsx structure.

    IMPORTANT: All imports from project files must use the @/ alias (e.g., `@/components/layout/TopNav`, `@/data/frameworks`).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -10</automated>
    <manual>Navigate to localhost -- TopNav renders with branding. Navigate to /module/gri -- breadcrumbs show "Dashboard > GRI Standards".</manual>
  </verify>
  <done>
    Route definitions work: "/" renders DashboardPage, "/module/gri" renders ModulePage. TopNav is persistent on both pages. Breadcrumbs appear only on module pages with correct framework name. All TypeScript compiles cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build DashboardPage with card grid, FrameworkCard, and ModulePage with two-panel skeleton</name>
  <files>
    src/components/dashboard/FrameworkCard.tsx
    src/pages/DashboardPage.tsx
    src/pages/ModulePage.tsx
  </files>
  <action>
    Build the two page components and the framework card, completing the user-facing application.

    1. Create `src/components/dashboard/FrameworkCard.tsx`:
       - Accept `framework: Framework` prop.
       - Wrap shadcn Card in a React Router `<Link to={/module/${framework.slug}}>`.
       - Use `group` class on the Link for group-hover patterns.
       - Card contents: icon in a rounded container (`bg-primary/10 text-primary`), CardTitle with framework name, CardDescription with framework description.
       - Hover effect: `group-hover:-translate-y-1 group-hover:shadow-lg transition-all duration-200` on the Card.
       - Card should have `h-full` to equalize heights in the grid.
       - Per locked decisions: NO status indicators, NO "Not Started" badges. Cards are clean.
       - Follow RESEARCH.md Pattern 5 closely.

    2. Create `src/pages/DashboardPage.tsx`:
       - Header section: minimal. A clean `<h1>` with something like "Sustainability Reporting Assessment" and a brief subtitle explaining the task (e.g., "Select a framework to begin your assessment"). Style with `text-2xl font-semibold tracking-tight` for the heading, `text-muted-foreground` for the subtitle.
       - Grid of 5 FrameworkCards: `grid grid-cols-1 gap-6 sm:grid-cols-2 lg:grid-cols-3`.
         This produces: 1 column on mobile, 2 columns on sm (2+2+1 layout), 3 columns on lg (3+2 layout) -- matching the locked decision for responsive behavior.
       - Import frameworks from `@/data/frameworks` and map to FrameworkCard components.
       - Per locked decisions: flat grid, no grouping labels or category dividers.

    3. Create `src/pages/ModulePage.tsx`:
       - Use `useParams()` to get `frameworkSlug`.
       - Look up framework from `frameworks` array. If not found, `<Navigate to="/" replace />`.
       - Framework intro section at top:
         - Icon in a larger container (`h-12 w-12`), framework name as `<h1>`, framework description.
         - Below: 1 short paragraph (3-4 sentences) providing a brief overview of the framework's purpose, primary audience, and structure. This is Claude's discretion per CONTEXT.md.
         - Content for each framework intro (write substantive but concise text):
           - GRI: Stakeholder-focused, modular Universal + Topic Standards, most widely used globally
           - ISSB: Investor-focused, TCFD-aligned four pillars, financial materiality
           - CSRD: EU regulatory directive, double materiality, broad scope
           - ESRS: Technical standards implementing CSRD, cross-cutting + topical structure
           - SBTi: Science-based emissions targets, scope 1/2/3, aligned with Paris Agreement
       - Separator after intro section.
       - Two-panel layout skeleton: `grid grid-cols-1 gap-8 lg:grid-cols-2` with two dashed-border placeholder panels. Left: "Assessment form coming in a future phase". Right: "Report preview coming in a future phase". Style with `border-dashed border-border rounded-lg p-8 text-center text-muted-foreground`.
       - Follow RESEARCH.md two-panel skeleton pattern.

    Per locked decisions:
    - Entire card is clickable (Link wraps Card) -- CHECK
    - Subtle lift/shadow on hover -- CHECK (translate-y + shadow-lg)
    - No per-framework accent colors -- CHECK (uniform primary color for all icons)
    - Module switching only through dashboard -- CHECK (no dropdown switcher)

    Visual polish details (FOUND-03):
    - Cards should feel "corporate-polished: consulting firm meets environmental report"
    - Use consistent spacing: gap-6 for card grid, gap-8 for two-panel layout
    - Typography hierarchy: h1 at text-2xl font-semibold, descriptions at text-muted-foreground
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
    <manual>Visit localhost in browser. Verify: 5 cards visible in grid, hover lift works on each card, clicking GRI card navigates to /module/gri with intro text and two-panel skeleton. Breadcrumbs update. Back to dashboard via nav or breadcrumb.</manual>
  </verify>
  <done>
    Dashboard shows 5 framework cards in a responsive grid (3+2 on desktop, 2+2+1 on tablet, stacked on mobile). Each card is fully clickable and navigates to the correct module page. Module pages show framework intro with icon, name, description, and overview paragraph, followed by a two-panel placeholder layout. Hover effects are smooth. No status badges on cards.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of dashboard and module pages</name>
  <files>None (verification only)</files>
  <action>
    Human verifies the complete application shell: dashboard with 5 themed framework cards, persistent top navigation with breadcrumbs, 5 module pages with framework intros and two-panel skeletons, all styled in the forest/earth corporate theme.

    Verification steps:
    1. Run `npm run dev` and open localhost in browser
    2. **Dashboard check:** Verify 5 framework cards appear in a clean grid. Cards should show framework name, description, and a distinguishing icon. Background should be warm off-white, not pure white. Cards should be white with subtle borders.
    3. **Hover check:** Hover over each card -- should see a subtle upward lift and shadow increase. Feels polished, not jarring.
    4. **Theme check:** Primary color should be deep forest green (not bright green). Typography should be Inter (clean sans-serif). Overall feel: "consulting firm meets environmental report."
    5. **Navigation check:** Click the GRI card -- should navigate to /module/gri. Top nav should persist with breadcrumbs showing "Dashboard > GRI Standards". Click "Dashboard" in breadcrumbs to return.
    6. **Module page check:** Framework intro section with icon, name, and overview paragraph. Below: two dashed-border panels (form placeholder, preview placeholder).
    7. **Responsive check:** Resize browser to tablet width (cards should stack 2 per row), then mobile width (1 per row).
    8. **Try all 5 modules:** Click through each framework card to confirm routing works for gri, issb, csrd, esrs, sbti.

    Resume signal: Type "approved" to proceed, or describe any visual/functional issues to fix.
  </action>
  <verify>Human visual inspection confirms all 8 checks above pass</verify>
  <done>User approves the visual design, layout, navigation, and theme of the complete application shell</done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds with zero errors
2. `npm run dev` serves the app on localhost
3. Dashboard at "/" shows exactly 5 framework cards in responsive grid
4. Clicking each card navigates to /module/{slug} (5 different routes all work)
5. TopNav persists on all pages with correct branding
6. Breadcrumbs appear on module pages, hidden on dashboard
7. Module pages show framework intro and two-panel skeleton
8. Invalid module slugs redirect to dashboard
9. Forest/earth theme is visually applied (not default shadcn gray)
10. Card hover effects work (lift + shadow)
</verification>

<success_criteria>
- Students see a polished, professional dashboard with 5 framework cards
- Clicking any card navigates to that module's page with framework intro
- Navigation is intuitive: persistent nav bar, breadcrumbs, click-to-navigate cards
- Visual theme is corporate-polished with forest/earth palette, not a default template
- Responsive layout works on desktop (3-col), tablet (2-col), and mobile (1-col)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-application-shell/01-02-SUMMARY.md`
</output>
